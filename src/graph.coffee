# GRAPHS
class Graph
  constructor: (input) ->
    graphSpec = spec

class Layer
  constructor: (layerSpec, statData) ->
    @spec = layerSpec
    @precalc = statData
  calculate: (statData) ->
    layerData

